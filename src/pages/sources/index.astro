---
import BaseLayout from '../../layouts/BaseLayout.astro'; import EntityIndex from '../../components/EntityIndex.astro'; import { publicEntries } from '../../utils/content'; const entries:any[]=await publicEntries('sources');
---
<BaseLayout title="Sources" description="Normalized public sources used by APT Notes."><EntityIndex title="Sources" description="Each source preserves publisher, authors, publication and access dates, source type, language and canonical URL." type="Source" base="/sources/" entries={entries}/></BaseLayout>
