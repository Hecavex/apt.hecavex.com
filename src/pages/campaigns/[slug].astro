---
import BaseLayout from '../../layouts/BaseLayout.astro';
import EntityProfile from '../../components/EntityProfile.astro';
import { loadKnowledge } from '../../utils/knowledge';

export async function getStaticPaths() {
  const knowledge = await loadKnowledge();
  return knowledge.campaigns.map((entry: any) => ({ params: { slug: entry.data.slug }, props: { entry } }));
}

const { entry } = Astro.props;
const knowledge = await loadKnowledge();
---
<BaseLayout title={`${entry.data.name} | Campaign`} description={entry.data.summary} type="ProfilePage" image={`/og/generated/campaign-${entry.data.slug}.png`} imageAlt={`${entry.data.name} campaign record by APT Notes`}>
  <EntityProfile entry={entry} collection="campaigns" knowledge={knowledge} />
</BaseLayout>
