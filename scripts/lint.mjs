import fs from'node:fs';import path from'node:path';const roots=['src','scripts','tests','templates'];const errors=[];const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.(astro|css|ts|mjs|md)$/.test(e.name)){const s=fs.readFileSync(p,'utf8');s.split(/\r?\n/).forEach((line,i)=>{if(/[ \t]+$/.test(line))errors.push(`${p}:${i+1}: trailing whitespace`);if(line.includes('\t'))errors.push(`${p}:${i+1}: tab character`)})}}};roots.filter(fs.existsSync).forEach(walk);if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log('Lint checks passed.');
